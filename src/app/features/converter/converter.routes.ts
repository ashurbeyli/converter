import { Routes } from '@angular/router';
import { ConverterComponent } from './components/converter/converter.component';

export const CONVERTER_ROUTES: Routes = [{ path: '', component: ConverterComponent }];